# 引き継ぎプロンプト(2026-08-29、AutoEIS 比較ラウンドの実行)

このファイルは次セッションの冒頭にそのまま貼るための作業メモ。用が済んだら更新するか削除してよい。
(注:このファイルは **git 管理下**。commit すると履歴に入る。)

---

AutoCircuit プロジェクト(`C:\Users\toshi\python\AutoCircuit`)の作業を継続する。
今回のタスクは **`docs/AUTOEIS_COMPARISON_PLAN.md` の実行**。

## まず読むもの(この順で)

1. **`docs/AUTOEIS_COMPARISON_PLAN.md`** — 今回やること。**`CLAUDE.md` の「Start here」にはまだ
   載せていない**(結果が出るまでは載せない、と計画の §5 step 5 に書いてある)ので、この
   プロンプトが唯一の入口。
2. **`CLAUDE.md`** — `### Purpose`。特に目的3点目「専門家が要った2工程の自動化」。今回の比較が
   **既定値どうしの比較**である理由がこれ。
3. **`docs/HANDOFF.md`** §3(再導出してはいけない実測)と §4(環境の癖)。最新は §31。
4. **`docs/SEARCH_ALGORITHM_SCREENING.md`** §1 と §4.2 — 壁時計予算と安いアリーナがどちらも
   嘘をつく話。今回の比較設計はこの2つの反省の上に立っている。

## 直近セッション(2026-08-29)でやったこと

**計画書 `docs/AUTOEIS_COMPARISON_PLAN.md` を書いただけ。実装ゼロ、測定ゼロ、コミットもまだ。**
作業ツリーの差分は `docs/AUTOEIS_COMPARISON_PLAN.md`(未追跡)とこのファイルの2本だけで、それ以外は
`523ed20` の時点で clean・push 済み。この2本のコミットは次セッションの最初のコミットに混ぜてよい。

出発点の事実:**AutoEIS は4箇所で先行研究・設計の出所として引用されているだけで、一度も走らせて
いない**(`docs/IMPLEMENTATION_PLAN.md:71,84,289,549`、`docs/SEARCH_ALGORITHM_SURVEY.md:85,99`、
`docs/DISCOVERY_V2_PLAN.md:421`、`src/autocircuit/core/discover.py:20`)。`benchmarks/` にアームも
無い。したがって今この repo が言える比較は全部「予想」であって、それは screening round が
survey の一位推薦をひっくり返す前と同じ状態。

## 最初にやること:ステップ0(go/no-go、1時間まで)

**アリーナを1つも作る前に、AutoEIS が動くかだけを確かめる。** 計画 §5 step 0 の5項目:

1. **隔離環境**に AutoEIS を入れる(専用の conda env / venv)。Julia(`juliacall` /
   EquivalentCircuits.jl)と JAX/NumPyro が付いてくる。**`pyproject.toml` には絶対に入れない**
   — numpy+scipy 縛りは出荷する wheel の話で、ベンチ専用の別環境なら構わないが、それは
   プロジェクト環境の外に置くという条件込み。
2. **素子の語彙を、この文ではなく入れた版から確認する。** 期待は `R,C,L,P(CPE),W`。
3. **プログラムから候補リストが取れるか**(notebook ではなく関数呼び出し)。
4. **シード制御**が GEP 側と NUTS 側にあるか。無ければ「無い」と結果ファイルに書く。
5. **1スペクトルの実行時間**を測る。これが以降のアリーナ全部のサイズを決める。

**1時間で入らなければ止めて、そう書く。** 「この機械のこの日、この版では入らなかった」は正当な
結果で、いまドキュメントにある「head-to-head は存在しない」という記述がそのまま残るだけ。
**文献比較を測定のふりをして代用しないこと。** なお Julia/JAX は WSL のほうが楽な可能性があるが、
**Bash ツールから `wsl` を呼んではいけない**(§4)ので、その経路はユーザーに実行してもらう。

## この比較で絶対に守ること(計画の要点、崩すと結果が無意味になる)

- **語彙外は N/A であって 0 ではない。** `SKINF` を含む参照系2本と `Wo` を含む1本は AutoEIS では
  表現できない。相手が持たない段(こちらの事後分布の不在も同じ)を点数にしたら、それは測定ではなく
  機能表。
- **既定値どうしで比べる。** チューニングしたアームは別扱い・別ラベルで、見出しには絶対にしない。
- **同値判定は両者に同じ審判(こちらの `canonical_form()` + `EQUIVALENCE_RTOL`)を当てる。**
  `R1-p(R2,C1)` と `p(R1,C1-R2)` は 1.2e-15 で同一。相手が後者を返したら「真値を見つけた」。
  自分にだけ同値検出を使ったら審判を採点していることになる。
- **本命はアリーナ C(第三者的にサンプリングした真値)。** A(この repo の参照系)は
  `LARGE_REFERENCES` のコメントが示すとおり**識別可能になるまで調整された**フィクスチャなので、
  A だけで回すと「誰が問題を作ったか」を測る。A と C が食い違ったら **C が結論**。
- **時間は説明であって見出しではない。** 速度比は結論のどの文にも書かない。壁時計は機械を測る
  (この repo だけで3回やらかしている)。
- **分解能 `d` を走らせる前に結果ファイルに書く。** 差が `d` の内側なら結論は「このシード数では
  区別がつかない」で、対応はシードを増やすか何もしないかのどちらかであって、**バーを書き直す
  ことではない**。島モデルは 480 シードまで行って p = 0.216 で棄却された。
- **`translate.py`(相手の回路文字列 → こちらの文法)にテストを書く。** ここのバグは症状を出さずに
  そのまま点数になる。入れ子の並列と CPE を含む手検算ケース、全真値の往復テスト。
- **`filtered`(相手の物理フィルタが真値を消した)を `wrong` に混ぜない。** `p(R1,C1)-p(R2,C2)` は
  直列 R を持たないので、フィルタ次第で探索に届かない。それは探索の失敗ではなく、それ自体が結果。
- **これはゲートではなく記述。この結果でこの repo の既定値は1つも変えない。**

## 今回の成果物

- `benchmarks/autoeis_round/`(`run_autoeis.py` / `run_autocircuit.py` / `translate.py` /
  `score.py` / `README.md`)。**2つの生産者と1つの採点者**で、環境は交わらせない
  (`score.py` は AutoEIS を import しない)。両方の結果 JSON に**全部の版**(AutoEIS、
  EquivalentCircuits.jl、Julia、numpyro/jax、numpy、scipy、この repo の git SHA)を記録する。
  無ければ再現不能なので測定ではない。
- `docs/AUTOEIS_COMPARISON.md`(結果。棄却した読みは消さずに並べる)。
- 更新:`IMPLEMENTATION_PLAN.md` の先行研究段落、`SEARCH_ALGORITHM_SURVEY.md` の 85 行目、
  `HANDOFF.md` の新節、**結果が出てから** `CLAUDE.md` の Start here 15 番。

## この比較が終わっても言えないこと(計画 §7)

合成の真値は部品ではない。これは**2つのツールの既定値を、2つの版で、1台の機械で**比べたもので、
「遺伝的プログラミング対網羅列挙」の比較ではない。事後分布の質は比較できないし、こちらに事後分布が
無いことを点数の入った文に混ぜてはいけない。引き分けはどちらの検証でもない。

## 比較ラウンドの外に残っている作業(前回からの持ち越し)

- **A. Lin-KK の降格**、**Gamry `.DTA` / BioLogic `.mpt` リーダー**、複数温度・複数バイアスの
  同時フィット(`interpret` だけが得をする唯一の縮退解消手段。`OBJECTIVE_PLAN.md` §8)。
  Fit 画面はまだどちらの objective レポートも出さない(CLI の `fit --objective` は出す)。
- **B. NSGA-II は「効くが急がない」**(凍結ランドスケープで中央値 256 対 308、どちらも 120/120)。
  **ALPS は追加しないこと**(24/30 に悪化)。
- **C. 探索の残り候補**:(g) beam/分枝限定、(h) VARPRO。pool の段階化は **CPE を本当に要する
  参照系のアリーナが作れるまで着手しない**。

## 守ること

- 会話は日本語、コード・コメント・ドキュメント・コミットメッセージはすべて英語。
- 単純な調査は haiku、単純な実装は sonnet に委任。**報告は信じず自分で読んで再実行する。**
- **緑のまま何も証明していない CI を疑う。** skip・0件・空集合は「合格」に見える。
- **安いアリーナで測って順位を決めない。**
- 実測で計画やゲートが誤っていたと判明したら、**黙って解釈を緩めず**計画側を訂正する。棄却した読み
  は消さずに並べる。
- **通知を測定として扱わない。ファイルとプロセス表だけが測定。**
- ステップ単位でコミットし、GitHub にプッシュする。**push = 公開**。

## 環境の癖(全文は `docs/HANDOFF.md` §4)

- パッケージは pip install されていない。`$env:PYTHONPATH = "C:\Users\toshi\python\AutoCircuit\src"`
  が必須。`python -m pytest` はリポジトリルートから。
- **PowerShell でソースファイルを書き換えないこと**(cp932 で `Ω` が化ける)。編集ツールか、
  UTF-8 を明示した `python - <<'EOF'` を使う。**ヒアドキュメント内のバックスラッシュ n は実際の
  改行になって届く。**
- CI は `ruff check .` と `mypy` を回す。`ruff format` は回していない。
- CPU を専有するベンチとテストを同時に走らせない(性能コア2)。**ブラウザを開いたまま回さない。**
- **長時間ジョブはハーネスの `run_in_background` で回し、結果は出力ファイルを読む。**
- **Bash ツールから `wsl` を呼んではいけない。** ngspice は WSL の Ubuntu-24.04 にのみある。
- フルスイートは専有時 10 分15 秒 / 942 pass(`--ignore=tests/test_spice_ngspice.py`)。

### Web UI

```powershell
cd web; npm run dev      # http://localhost:5173(Python は起動時に固まる。編集したら再起動)
npm run smoke            # Python 側の経路を Pyodide でヘッドレス検証
npm run check            # tsc + 回路図ジオメトリ + サンプル照合
```

- **bridge の操作を増やしたら `src/autocircuit/web/light.py` と `web/src/worker/protocol.ts` の
  `BRIDGE_VERSION` を両方上げる。** ただし**同じ編集で版を上げてもフィールド追加の skew は
  捕まらない**(§26)。
- **ブラウザ自動化のスクリーンショットとダウンロードは `.playwright-mcp/` に落ちる。終わったら消す。**
