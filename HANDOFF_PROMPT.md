# 引き継ぎプロンプト(2026-08-23、探索アルゴリズムの絞り込み測定 時点)

このファイルは次セッションの冒頭にそのまま貼るための作業メモ。用が済んだら更新するか削除してよい。
(注:このファイルは **git 管理下**。commit すると履歴に入る。)

---

AutoCircuit プロジェクト(`C:\Users\toshi\python\AutoCircuit`)の作業を継続する。

## まず読むもの(この順で)

1. **`CLAUDE.md`** — `## Project overview` の `### Purpose` と `### Objectives` が以降の設計判断の
   決定基準。特に:
   - 目的3点(データから回路を探す / 回路は手段で目的は部品の中身 / 専門家が要った2工程の自動化)
   - 帰結:**入力は周波数とインピーダンスだけ**。形状も型番も部品種別も受け取らない・聞かない。
   - 帰結:**幾何不要な量は対象、絶対的な材料定数は対象外(永久に)**。
   - `### Objectives`:objective はレポートだけを変え、数値には絶対に触れない。ゲート O1 は**未実装**。
   - 「Start here」に **13番(探索アルゴリズムの survey と screening)** を追加済み。
2. **`docs/SEARCH_ALGORITHM_SCREENING.md`** — 直近セッションの成果。**下の「次の作業」はこの文書の
   §5 がそのまま根拠**なので、着手前に §4.2・§4.6・§5 は必ず読む。
3. **`docs/HANDOFF.md`** — 現状。§22(`core/descriptors.py`)、§21(`core/interpret.py`)、
   §3(再導出してはいけない実測)、§4(環境の癖)、§20(遺伝的探索のゲート、**作業中**)。
4. 触る部分の plan doc(`CLAUDE.md` の「Start here」1〜13)。

## 直近セッション(2026-08-23)でやったこと

**コミット2本、HEAD は `cdfe394`。** 探索アルゴリズムの**絞り込み測定**を1ラウンド実施した。

- `6b3f01c` 前セッションの `docs/SEARCH_ALGORITHM_SURVEY.md` を commit(測定ゼロの地図)。
- `cdfe394` `docs/SEARCH_ALGORITHM_SCREENING.md` と `benchmarks/screening_round/`。

### なぜ新しい計器を作ったか

`benchmarks/discovery_v2.py evolve-gate` は1アーム 2.5〜3時間。**予算が壁時計で、見るトポロジーを
その場でフィットする**ので、独立な3つの量が1つの合格率に融合してしまう ——(F1) 1評価の秒数、
(F2) 必要な評価回数、(F3) 評価が正しく採点できるか。候補12件の絞り込みにこれは使えない。

**凍結ランドスケープ**:`_evolve` が探索する空間の全トポロジーを一度だけ tier-1 スクリーニングして
テーブルに落とす。以後トポロジー探索は**テーブル参照だけの探索**になり、**予算単位が「フィット回数」**
なので機械負荷から独立し、シード数百本が無料になる。`arms.py` は `_next_generation` / `mutate` /
`crossover` / `_tournament` を**直接駆動**する(再実装するとハーネスを測る)。

### 引き継ぐべき結論(すべて実測)

**期待に反した4つ。**

- **criterion は原因ではない。** 真値の同値類は作った全アリーナで 1〜17 位。CPE アリーナで上位を
  占める16件も真値自身の CPE 置換体。
- **素子数上限は原因ではない。** 6 対 7 で差なし。pool 内では同値類が空間と一緒に増えるため。
- **C++ / Cython / numba では埋まらない。** インピーダンスカーネルは1スクリーニングの 36〜47% で
  すでに DE 母集団全体に償却済み。Amdahl で上限 1.4〜1.7倍、必要な差は 13倍。しかも
  numpy+scipy のみという規約(=ブラウザビルドの根拠)を失う。
- **CMA-ES は現行 DE に負ける。** 現行を半分の予算にしたほうがまだ良い(20/26 @1,344 対 16/26 @1,640)。

**原因は CPE で、経路が2つある。** 1フィット **1.77 s 対 0.87 s**、かつ真値クラスの密度が
**0.55% → 0.085%**(pool が空間を10倍にする一方、厳密同値体は1.5倍しか増えない)。合わせて約13倍。

**§4.2 がこのリポジトリの A/B 全般に効く教訓。** 11,033 トポロジーのアリーナでは**全アームが 30/30 で
同着**、21,057 では**分離する**。安いアリーナで止めていたら「現行を上回る候補なし」と報告して誤って
いた —— `screen` 対 `screen-rank` と同じ形。現行を上回るのは3アーム(**120/120 対 87/120**、区間非重複)
で、共通点は演算子ではなく**繁殖する集合を有界にしていること**。`EVOLVE_SEARCH_PLAN.md` §1.2 の
診断が測定として出た形。

## 再導出してはいけない実測(今回追加分。既存分は `docs/HANDOFF.md` §3・§22)

- **サンプリングは順位ではない。** RCLCPE 1,176点の一様サンプルは「真値を上回るものゼロ」と答えたが、
  実際は16件ある(n=6 層の 0.09%、400点サンプルの期待検出数 0.35)。順位は全列挙からしか出ない。
- **コスト近接は同値ではない。** 「真値のコストの5%以内」を目標集合にすると CPE アリーナで
  **7.6倍(136 対 18)** 過大計上し、**判定が1件ひっくり返った**(MAP-Elites が 118/120 → 120/120)。
  目標集合は必ず `targets.py`(応答検証)で作ること。
- **`Circuit.canonical_form()` は `Circuit.parse` に渡せない。** `[p(C,R)-p(C,R)-p(C,R)]` はラベルで
  あって回路式ではない。渡すと `CircuitSyntaxError`。`except` で握ると**空集合が合格に見える**
  (実際に1セッション分そうなった)。refit には `circuit.to_string()` を使う。
- **長い計算は `landscape2.py`(JSONL 逐次追記・再開可能)で回す。** `Start-Process` で切り離しても
  ハーネス外から**約10分で殺される**(20分級のビルドを2回失った)。**前景実行(`timeout` を
  560000 まで指定)なら殺されない。**
- **ログの最終行だけで「殺された」と判断しない。** 51分のランドスケープビルドを死んだと誤診したが
  完走していた。§4.2 の結論はそのアリーナに立っている。
- **`evolve_probe.py` は自分の refit で重くなる。** 上限9素子だと探針のフルフィット80本が本体より
  遅い。同じ信号は `reach_probe.py`(クラスのスクリーニングコストは完全同値なので最良コストだけ見る)
  で100分の1のコストで取れる。

## 次の作業(この順。根拠は `docs/SEARCH_ALGORITHM_SCREENING.md` §5)

**A.(最優先)報告経路の欠陥を確認して直す。** §4.6 の1行は測定ではなく欠陥:上限9・seed 0 で真値
クラスが**9回訪問され、270フィット中スコア1位に採点され、1件も `candidates` に載らなかった**。
`_shortlist_candidates` か `_refine` のどちらが落としたかを特定する(`n_refine=30` の per-size クォータ
がサイズ2〜9に薄く割れた、または `REFIT_HEADROOM` の期限で切れた、が第一候補)。EV2 と同型の
**返り値に対するテスト**で固定する。**探索の改善はこの後。**1位に採点された候補を落とすショート
リストがある限り、探索を良くしても報告には出ない。再現コマンドは
`benchmarks/screening_round/README.md`。

**B. `_evolve` の繁殖プールを有界化する**(`EVOLVE_SEARCH_PLAN.md` ステップ4の前半)。
`_next_generation` の呼び出し側で `alive` を「Pareto front + スコア上位 population 件」に絞るだけ。
実測 120/120 [0.97,1.00]、中央値 308 フィット(現行 87/120 [0.64,0.80]、451)。
ゲート:EV1 のラチェットに加えて、`benchmarks/screening_round/arms.py` の 120 シード比較を再現。
**EV5(網羅段のバイト一致)を必ず回す** —— `_next_generation` は網羅段からは呼ばれないはずだが、
それを確認するのがゲートの仕事。

**C. NSGA-II と MAP-Elites は「効くが後回し」。** どちらも 120/120 だが、NSGA-II は選択機構の
書き換え(中央値 256 で最速)、MAP-Elites は最も遅く最も大きい(418)。**ALPS は追加してはいけない**
(24/30 に悪化)。B を入れた後に、まだ足りなければ NSGA-II。

**D. `_evolve` の pool 段階化(survey に無い候補)。** `descriptors.choose_pool` が網羅段でやっている
「狭く始めて証拠が出たら広げる」を fallback にも適用する。実測で **0/1 → 2/2**。
**ただし真値が本当に CPE を要する参照系で安全性を測るまで採用しない** —— それが
`LARGE_REFERENCES[1]`(capacitor + interfacial)で、今回アリーナを作れなかった参照系。

**E. 保留で生きている候補。**(g) beam / 分枝限定(決定的、86〜220フィット、レベルごとにコスト有界。
ただし1アリーナ1本は合格率ではないので確率版が要る)、(h) VARPRO(§4.6 が特定した「1フィットの
単価」を直接攻める唯一の候補)、**§4.5 の段取り 23〜33%**(カーネルでも最適化器でもない素の Python、
誰も見ていない)。

**F. 以前からの残件。** objective の配線とゲート O1 / `discover` レポートへの interpretation 接続 /
ブラウザへの pool 自動選択の配線(**公開サイトに違反が残っている**)/ Lin-KK の降格 /
Gamry `.DTA`・BioLogic `.mpt` リーダー。詳細は `docs/HANDOFF.md` §6。

## 作業ツリーの注意(引き継ぎ時点)

**`benchmarks/search_screening.py` と `benchmarks/_landscape/` は未追跡のまま残してある。私(前セッション)
が書いたものではない。** 今回の実験とほぼ同じ内容(ランドスケープ構築・各アーム・KPI-0・Wilson 区間・
最適化器 A/B)を独立に実装したもので、セッション中も更新され続けていた(16:25→17:10 で 44 KB→50 KB)。
同じリポジトリで別の書き手が並行作業していた可能性がある。**触らずに残した。**
`benchmarks/screening_round/` と重複するので、統合するか片方を捨てるかは出所を確認してから決めること。

**凍結ランドスケープの本体(`land_*.json`、3.4 MB)は `.gitignore` 済み**(`benchmarks/pyodide` と同じ
規約:生成物は再構築、結果は文書に書く)。`targets_*.json` は小さく高価(CPE 版は約50分)なので
commit してある。再構築コマンドと実測時間は `benchmarks/screening_round/README.md`。

## 守ること

- 会話は日本語、コード・コメント・ドキュメント・コミットメッセージはすべて英語。
- 単純な調査は haiku、単純な実装は sonnet に委任。委任時は必ず「テストが落ちたらアサーションを
  弱めず、ライブラリのバグを疑って xfail で残し報告せよ」と指示する。**報告は信じず自分で読んで
  再実行する。**
- 完全性保証がモード2と discovery v2 の存在意義。フィルタや枝刈りは「疑わしきは残す」。
- **緑のまま何も証明していない CI を疑う。** skip・0件・空集合は「合格」に見える。今回**また起きた**
  (正準形を `fit` に渡して全件例外 → 「クラス到達0件」)。
- **安いアリーナで測って順位を決めない。** 今回、11,033 では全アーム同着で 21,057 で分離した。
- 実測で計画やゲートが誤っていたと判明したら、**黙って解釈を緩めず**計画側を訂正する。
  `docs/SEARCH_ALGORITHM_SCREENING.md` は棄却した読み(サンプリング版・コスト近接版)を**消さずに
  並べて**ある。
- ステップ単位でコミットし、GitHub にプッシュする。**push = 公開**(`pages.yml` が `main` への push
  ごとに <https://toshihiroiguchi.github.io/AutoCircuit/> を更新)。
- **UI は必ず実ブラウザで確認する。**

## 環境の癖(全文は `docs/HANDOFF.md` §4)

- パッケージは pip install されていない。`$env:PYTHONPATH = "C:\Users\toshi\python\AutoCircuit\src"`
  が必須。`python -m pytest` は必ずリポジトリルートから。フルスイートは約6分(専有時)。
- **PowerShell でソースファイルを書き換えないこと。**`Get-Content -Raw` は cp932 として読むので
  `Ω` が化ける。編集ツールか、符号化を明示した Python を使う。
- **Bash ツールのヒアドキュメントはアポストロフィを含む長文で壊れることがある。** ソースファイルの
  新規作成は Write ツール、既存ファイルの書き換えは `python - <<'EOF'` に UTF-8 明示で。
  PowerShell の here-string(`@'...'@`)を bash に書くと**リテラルの `@` が本文に入る**(今回やった)。
- CI は `ruff check .` と `mypy` を回す。**`ruff format` は回していない**。新規ファイル以外を
  `ruff format` にかけると無関係な巨大 diff になる。`benchmarks/` も `ruff check` の対象。
- CPU を専有するベンチとテストを同時に走らせない(Core 7 150U、性能コア2)。**同時に走らせると
  実測で 2倍以上遅くなる。**
- **長時間ジョブは前景で `timeout` を大きく取って回す。** バックグラウンド(harness の
  `run_in_background` も PowerShell の `Start-Process` も)は約10分で外から殺される。
- **Bash ツールから `wsl` を呼んではいけない。** Git Bash が `/mnt/c/...` を書き換える。
  PowerShell から呼ぶこと。ngspice は WSL の Ubuntu-24.04 にのみある。

### Web UI

```powershell
cd web; npm run dev      # http://localhost:5173
npm run smoke            # Python 側の経路を Pyodide でヘッドレス検証
npm run build            # -> web/dist/
npx tsc --noEmit -p tsconfig.json
```

- **bridge の操作を増やしたら `src/autocircuit/web/bridge.py` と `web/src/worker/protocol.ts` の
  `BRIDGE_VERSION` を両方上げる。** 片方だけだとページが起動を拒否する(設計どおり)。
- **ブラウザ自動化のスクリーンショットとダウンロードはリポジトリ直下(`.playwright-mcp/`)に落ちる。
  終わったら消す。**
