# 引き継ぎプロンプト(2026-08-22、既定 pool のスペクトル由来化 時点)

このファイルは次セッションの冒頭にそのまま貼るための作業メモ。用が済んだら更新するか削除してよい。
(注:このファイルは **git 管理下**。commit すると履歴に入る。)

---

AutoCircuit プロジェクト(`C:\Users\toshi\python\AutoCircuit`)の作業を継続する。

## まず読むもの(この順で)

1. **`CLAUDE.md`** — `## Project overview` の `### Purpose` と `### Objectives` が以降の設計判断の
   決定基準。特に:
   - 目的3点(データから回路を探す / 回路は手段で目的は部品の中身 / 専門家が要った2工程の自動化)
   - 帰結:**入力は周波数とインピーダンスだけ**。形状も型番も部品種別も受け取らない・聞かない。
     必要な情報はスペクトルの形から導出する。`pool` と `skeleton` は「ユーザが明示的に狭める
     任意オプション」としてのみ存続。
   - 帰結:**幾何不要な量は対象、絶対的な材料定数は対象外(永久に)**。Z(f) は容量を決めるが
     誘電率は決めない(差は A/d)。
   - `### Objectives`:`model` / `interpret` の2目的。**objective はレポートだけを変え、数値には
     絶対に触れない。** ゲート O1(両 objective で `DiscoveryResult` がバイト一致)は**未実装**。
2. **`docs/HANDOFF.md`** — 現状。**§22(新設、`core/descriptors.py`)**、§21(`core/interpret.py`)、
   §3(再導出してはいけない実測)、§4(環境の癖)、§20(遺伝的探索のゲート、**作業中**)。
3. 触る部分の plan doc(`CLAUDE.md` の「Start here」1〜12)。

## 前セッションでやったこと

### 1. `core/interpret.py`(commit 済み)

フィット済み回路を内部構造として読む。全量に **invariant / form-dependent** のラベル。
ゲート I1 は**後半が本体** — 不変量が一致することだけでなく、form-dependent が**食い違うこと**も
試す。反証できないラベルはラベルではない。詳細は `docs/HANDOFF.md` §21。

### 2. 既定 pool のスペクトル由来化(`docs/POOL_FROM_SPECTRUM_PLAN.md`、`docs/HANDOFF.md` §22)

`CLAUDE.md` が明記していた未修正違反 —— 既定 pool `("R","C","L","CPE")` が拡散素子を黙って
除外していた —— を解消した。`discover(pool=None)` と CLI `--pool auto` が新既定。

**設計の芯は、トリガーが2つの読みの OR であること。両方とも単独トリガーとして実測で棄却された。**

| 真値 | 形状(45°区間 ≥0.75 dec) | 残差(runs z < −1.5, limit 5) |
|---|---|---|
| `R1-Ws1` | **24/24** | −2.07 / −0.77 / −1.03 → **1/3** |
| `R1-Wo1` | 24/24 | −0.77 / −2.07(limit 4 では −4.39)|
| `R1-p(R2,CPE1)-Wo1` | **0/24** | **−5.42** |
| 拡散なし8真値 | **192試行で偽陽性ゼロ** | −0.26 / −0.26 / +0.00 |

太字2行が設計そのもの。片方を削っても他のゲートは全部通り、答えの1クラスが黙って消える。
ゲート C6 がその2ケースを固定している。

## 再導出してはいけない実測(全文は `docs/HANDOFF.md` §22)

- **`W` は追加してはいけない。** CPE の `n=0.5` が**厳密に**半無限 Warburg。`R1-W1` スペクトルで
  真値 1.3344%、`R1-CPE1` 1.3344% と5桁一致。pool は既に別名で持っている。
- **`Ws`/`Wo`/`G` は伝送線路で、有限の R/C/L/CPE 木では再現できない。** 既定 pool の最良4パラメータ解は
  ノイズ床の 4〜17倍。7パラメータ使っても届かない。ここが機能の存在理由。
- **形状検出器で `arg Z` を使ってはいけない。** 直列 R が高域の 45° 枝を隠し、`R1-Wo1` と `R1-G1` を
  完全に取り逃がす。加算定数が消える微分量、局所 Nyquist 角
  `atan2(dIm/dlnw, -dRe/dlnw)` を使う。**符号は進行方向**(Nyquist は周波数が下がる向きに進む)。
  逆にすると全ケースで「枝なし」になる。
- **系統的残差の原因は2つあり、runs 検定は区別できない** —— pool が狭いか、素子数上限が低いか。
  両者の対処は逆向き(pool を広げると完全性レベルを失う)。limit 4 では拡散なしの
  `R1-p(R2,C1)-p(R3,C2)` が 24.191% で発火する。limit 5 では −0.26 で曖昧さは消えるが、
  代わりに残差の信号もほぼ消える(8パラメータ CPE スタックがノイズ床に届くため)。
- **`RUNS_Z_LIMIT = -3.0` を流用してはいけない。** あれは KK 検証用(偽陽性が「測定が悪い」と
  言ってしまう場面)の定数。ここは別定数 `POOL_WIDENING_RUNS_Z = -1.5`。低く置ける理由は非対称性 ——
  偽陽性は2度目の探索の時間だけで**報告される数値は変わらない**(基底 pool の候補を統合保持し、
  `base_complete_up_to` に狭い pool の完全性が残る)。偽陰性は8パラメータの代用品を返し、
  報告のどの統計もそれを示さない。
- **pool を全部入れるのは不可能。** 5素子までのトポロジー数は pool 4 で 2,976、6 で 31,712、
  8 で 143,156。`max_candidates=20000` 下で `+Wo` は完全性 5 を保つが `+Ws,G` は 4 に落ちる。
  `Ws` と `G` は入れ替えると相対誤差 3.3〜3.6倍で**代替不可**なので、両方入れるしかない。
- **`mypy` は `tests/` を見ない**(`packages = ["autocircuit"]`)。`choose_pool` の署名を bool から
  3値リテラルに変えたとき、テストが `True`/`False` を渡したまま非トリガー分岐に落ち、
  **ゲート C1/C3 のトリガー側が空検証**になっていた。スイートは green のまま。
- **`final_restarts=5` は拡散素子入りでは足りないことがある。** `R1-p(R2,C1)-G1` を真のトポロジーで
  フィットして `restarts=5, seed=1` で 15.055%、`restarts=20` で 1.313%。5 は `discover` の既定。

## 未着手・要注意

- **ブラウザが未配線。** `web/bridge.py` と `web/job.py` は具体 pool を取り既定が `DEFAULT_POOL` なので、
  デプロイ済みサイトは**この修正が直した違反をそのまま残している**。`DiscoveryJob` は再開・中断可能な
  ジェネレータなので、2段構成の配線は一行では済まない。
- `R1-p(R2,C1)-Ws1` は表中で最も弱いケース(形状 17/24、残差 −2.58 が1シードのみ)。3つ目の
  計器が必要になったらこのケースに対して作ること。
- `CC`/`HN`/`SKINF`/`SKINW` は既定 pool で表現できるか未測定なので `WIDENING_CANDIDATES` に入れていない。

## 次の作業(候補・要相談)

**A. objective の配線とゲート O1。** `model` / `interpret` を**レポート層だけ**に通す。
`discover()` / `fit()` は受け取らない。ゲート O1 は EV5 と同じフィンガープリント方式。
**実装より先にゲートを張ると、上流に漏れた瞬間に落ちる。**

**B. `discover` のレポートに interpretation を接続。** いま `fit` にしか無いが、トポロジーが
「発見された」場所こそ効く(等価クラスの但し書きが生きるのはそこ)。ブラウザ表示も未着手。

**C. ブラウザに pool 自動選択を配線。** 上記のとおり、公開サイトに違反が残っている。

**D. Lin-KK の降格。** 提案は (1) 共振で誤検出しない安価な前段チェック、(2) 共振検出時は最初から
`inconclusive`、(3) 掃引2本の比較、(4) KK 残差から σ(f) を推定して `weighting="sigma"` に流す
(**σ を作る側が repo のどこにも無い**)。

**E. トポロジー探索アルゴリズム。** 網羅列挙(≤5素子)は G1 30/30 で優秀。問題は **GA(6素子以上)が
真値回収 1/9**。効果順:(1) 回路文法で空間を狭めて 6-7素子でも網羅を届かせる、(2) GA を
「素子1個追加」の beam search に置き換える、(3) `EVOLVE_SEARCH_PLAN.md` §6 の「GA が正しい道具で
ないなら auto は under-fit な網羅結果を正直に返すべき」。着手前に docs を1本書くこと。

**F. 既存の低優先残件。** Gamry `.DTA` / BioLogic `.mpt` リーダー(実ファイル要)。コールドスタートの
続き。詳細は `docs/HANDOFF.md` §6。

## 守ること

- 会話は日本語、コード・コメント・ドキュメント・コミットメッセージはすべて英語。
- 単純な調査は haiku、単純な実装は sonnet に委任。委任時は必ず「テストが落ちたらアサーションを
  弱めず、ライブラリのバグを疑って xfail で残し報告せよ」と指示する。**報告は信じず自分で読んで
  再実行する。**
- 完全性保証がモード2と discovery v2 の存在意義。フィルタや枝刈りは「疑わしきは残す」。
- **緑のまま何も証明していない CI を疑う。** skip・0件・空集合は「合格」に見える。
  今回それが**実際に起きた**(上記 `mypy` は `tests/` を見ない件)。
- 実測で計画やゲートが誤っていたと判明したら、**黙って解釈を緩めず**計画側を訂正する。今回
  `POOL_FROM_SPECTRUM_PLAN.md` §3 は棄却した結論を**残したまま** §6 で覆した測定を並べてある。
- ステップ単位でコミットし、GitHub にプッシュする。**push = 公開**であることを忘れない
  (`pages.yml` が `main` への push ごとに <https://toshihiroiguchi.github.io/AutoCircuit/> を更新)。
- **UI は必ず実ブラウザで確認する。**

## 環境の癖(全文は `docs/HANDOFF.md` §4)

- パッケージは pip install されていない。`$env:PYTHONPATH = "C:\Users\toshi\python\AutoCircuit\src"`
  が必須。`python -m pytest` は必ずリポジトリルートから。フルスイートは約6分(専有時)。
- **PowerShell でソースファイルを書き換えないこと。**`Get-Content -Raw` は cp932 として読むので
  `Ω` が化ける。編集ツールか、符号化を明示した Python を使う。
- **Bash ツールのヒアドキュメントはアポストロフィを含む長文で壊れることがある。** ソースファイルの
  新規作成は Write ツール、既存ファイルの書き換えは `python - <<'EOF'` に UTF-8 明示で。
- CI は `ruff check .` と `mypy` を回す。**`ruff format` は回していない**(`cli/main.py` は元から
  format 非準拠)。新規ファイル以外を `ruff format` にかけると無関係な巨大 diff になる。
- CPU を専有するベンチとテストを同時に走らせない(Core 7 150U、性能コア2)。**同時に走らせると
  実測で 2倍以上遅くなる。** 長時間の計測はデタッチ起動(`nohup ... &`)。
  **harness のバックグラウンドシェルは10分で殺される。**
- **Bash ツールから `wsl` を呼んではいけない。** Git Bash が `/mnt/c/...` を書き換える。
  PowerShell から呼ぶこと。ngspice は WSL の Ubuntu-24.04 にのみある(Windows 側では 19 件 skip)。

### Web UI

```powershell
cd web; npm run dev      # http://localhost:5173
npm run smoke            # Python 側の経路を Pyodide でヘッドレス検証
npm run build            # -> web/dist/
npx tsc --noEmit -p tsconfig.json
```

- **bridge の操作を増やしたら `src/autocircuit/web/bridge.py` と `web/src/worker/protocol.ts` の
  `BRIDGE_VERSION` を両方上げる。** 片方だけだとページが起動を拒否する(設計どおり)。
- **ブラウザ自動化のスクリーンショットとダウンロードはリポジトリ直下(`.playwright-mcp/`)に落ちる。
  終わったら消す。**
